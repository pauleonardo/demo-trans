import styled from 'styled-components';

export const styles = {
  github: { color: 'initial', marginRight: '20px' },
  initial: { color: 'initial' },
};

export const FixedContainer = styled.div`
  position: fixed;
  width: 100%;
  bottom: 0;
`;
